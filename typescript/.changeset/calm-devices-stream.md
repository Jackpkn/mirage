---
'@struktoai/mirage-core': patch
'@struktoai/mirage-node': patch
'@struktoai/mirage-browser': patch
---

Model `/dev/null` and `/dev/zero` as character devices, including endless bounded-memory zero streaming, ranged reads, device metadata, and matching shell-command behavior.
